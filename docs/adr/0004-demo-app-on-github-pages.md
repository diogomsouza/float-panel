# Demo App on GitHub Pages

The project will include a separate Angular Demo App published to the `gh-pages` branch at `https://diogomsouza.github.io/float-panel/`. This follows the existing flex-layout release model, keeping npm package publishing and public demo deployment separate while using the same repository for version history.
