# Single Open Float Panel

Only one Float Panel should be open at a time. This avoids stacked floating filter panels and ambiguous outside-click behavior when several triggers exist on the same page, while still allowing each trigger to own its own panel instance.
