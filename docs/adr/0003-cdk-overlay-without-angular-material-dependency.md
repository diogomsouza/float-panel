# CDK Overlay Without Angular Material Dependency

The Float Panel package will use Angular CDK Overlay for positioning and overlay lifecycle behavior, but it will not depend on Angular Material. This keeps the library usable with any consumer UI kit while still relying on Angular's proven overlay infrastructure instead of hand-rolled positioning.
